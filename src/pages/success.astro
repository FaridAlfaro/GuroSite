---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Success">
    <section>
        <h1>Enviado con <span>éxito</span></h1>
        <p>Nos pondremos en contacto contigo a la brevedad</p>
    </section>
</Layout>

<style>
    section {
        height: 100vh;
        padding: 0 5%;
        display: flex;
        justify-content: center;
        align-items: center;
        background: transparent;
        flex-direction: column;
        background: var(--contrast);
        h1 {
            color: var(--background);
            font-size: 4em;
            font-weight: normal;
        }
        span {
            font-style: italic;
            font-weight: bold;
        }
        p {
            color: var(--background);
            font-weight: bold;
            font-size: 1em;
        }
    }
    @media (max-width: 700px) {
        section {
            background: var(--contrast);
            height: 100vh;
            padding: 0 5%;
            h1 {
                color: var(--background);
                font-size: 40px;
                font-weight: normal;
            }
            span {
                font-style: italic;
                font-weight: bold;
            }
            p {
                color: var(--background);
                font-weight: bold;
                font-size: 14px;
            }
        }
    }
</style>
