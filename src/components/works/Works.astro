---
---

<section class="works-section" aria-labelledby="works-title">
    
      
    <ul class="works-list">
      <li class="work-entry">
        <article>
          <div class="work-index">
            <div class="work-text">
            <span class="work-service"><strong>01</strong>Diseño de marca</span>
              <h3 class="work-title">Libre</h3>
            </div>
            <img src="https://th.bing.com/th/id/OIP.veOfD2wxZ9M7bRpUsbbIsAAAAA?rs=1&pid=ImgDetMain" alt="Diseño de marca para Libre" class="work-image" />

          </div>
        </article>
      </li>
  
      <li class="work-entry">
        <article>
          <div class="work-index">
            <div class="work-text">
            
            <span class="work-service"><strong>02</strong>Desarrollo web</span>
              <h3 class="work-title">Budget</h3>
            </div>
            <img src="https://th.bing.com/th/id/OIP.veOfD2wxZ9M7bRpUsbbIsAAAAA?rs=1&pid=ImgDetMain" alt="Desarrollo web para Budget" class="work-image" />

          </div>
        </article>
      </li>
  
      <li class="work-entry">
        <article>
          <div class="work-index">
            
            <div class="work-text">
              <span class="work-service"><strong>03</strong>Marketing digital</span>
              <h3 class="work-title">Nova</h3>
            </div>
            <img src="https://th.bing.com/th/id/OIP.veOfD2wxZ9M7bRpUsbbIsAAAAA?rs=1&pid=ImgDetMain" alt="Marketing digital para Nova" class="work-image" />

          </div>
        </article>
      </li>
  
      <li class="work-entry">
        <article>
          <div class="work-index">
            <div class="work-text">
              <span class="work-service"><strong>04</strong>Estrategia UX/UI</span>
              <h3 class="work-title">Forma</h3>
            </div>
            <img src="https://th.bing.com/th/id/OIP.veOfD2wxZ9M7bRpUsbbIsAAAAA?rs=1&pid=ImgDetMain" alt="Estrategia UX/UI para Forma" class="work-image" />

          </div>
        </article>
      </li>
    </ul>
    <a href="/works" class="btn modifier">Proyectos</a>
  </section>

  <script type="module">
  
    const images = document.querySelectorAll('.work-image');
  
    function animateClipPath(img, progress) {
      const maxCut = 100;
      const easedProgress = Math.min(Math.max(progress, 0), 1);
      const cut = easedProgress * maxCut;
      img.style.clipPath = `inset(0% 0% ${cut}% 0%)`;
    }
  
    window.addEventListener('scroll', () => {
      const viewportHeight = window.innerHeight;
  
      images.forEach(img => {
        const rect = img.getBoundingClientRect();
        const imgBottom = rect.bottom;
        const imgHeight = rect.height;
  
        // solo se aplica si la imagen ya pasó el centro de la pantalla hacia arriba
        if (imgBottom < viewportHeight / 2) {
          const distanceFromBottom = viewportHeight - imgBottom;
          const progress = distanceFromBottom / viewportHeight;
          animateClipPath(img, progress);
        } else {
          // restablece clip cuando la imagen está centrada o descendiendo
          img.style.clipPath = `inset(0% 0% 0% 0%)`;
        }
      });
    });
  </script>

  <style>
    .works-section {
      padding: 0% 5%;
      color: var(--contrast);
    }
    .works-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }
  
    .work-entry {
      width: 100%;
      margin: 0 auto 3rem;
      border-bottom: 1px dashed rgba(0, 4, 247, 0.295);
      padding-bottom: 2rem;
    }
  
    .work-index {
      font-size: 0.9rem;
      font-weight: 400;
      margin-bottom: 0.3rem;
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      color: var(--contrast);
    }
  
    .work-text {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: flex-start;
      gap: 0.5rem;
      color: var(--contrast);
    }
  
    .work-title {
      font-size: 3rem;
      font-weight: 600;
      margin: 0;
      padding: 0;
      text-transform: uppercase;
      letter-spacing: -1px;
      color: #000;
    }
    strong{
    font-size: .9rem;
    font-weight: 200;
    margin-right: 1rem;
  }
    .work-service {
      font-size: 0.9rem;
      color: var(--contrast);

    }
  
    .work-image {
      width: 80vw;
      height: calc(80vw / 1.618);
      margin-top: calc(1.618rem * 2);
      border-radius: 3px
      box-shadow: 0 8px 16px rgba(0,0,0,0.1);
      object-fit: cover;
      transition: clip-path 0.5s ease-out;
      clip-path: inset(0% 0% 0% 0%);
    }
  
    .sr-only {
      color: var(--contrast);
    }
    
    .modifier{
      display: flex;
      justify-content: center;
      align-items: center;
    }
    @media (min-width: 1280px) {
      .works-section {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      padding: 0% 5%;
      color: var(--contrast);
    }
    .works-list {
      display: flex;
      flex-direction: column;
      list-style: none;
      justify-content: center;
      align-items: center;
      width: 100%;
      padding: 0;
      margin: 0;
    }
  
    .work-entry {
      width: 100%;
      border-bottom: 2px dashed rgba(36, 0, 240, 0.199);
      padding-bottom: 2rem;
    }
  
    .work-index {
      font-size: 2rem;
      font-weight: 400;
      margin-bottom: 2rem;
      display: flex;
      justify-content: space-around;
      flex-direction: row;
      width: 100%;
      gap: 0.5rem;
      color: var(--contrast);
    }
  
    .work-text {
      display: flex;
      height: 5%;
      flex-direction: column;
      align-items: flex-start;
      gap: 0.5rem;
      color: var(--contrast);
    }
  
    .work-title {
      font-size: 6rem;
      font-weight: 600;
      margin: 0;
      padding: 0;
      text-transform: uppercase;
      letter-spacing: -1px;
      color: #000;
    }
  
    .work-service {
      font-size: 2rem;
      color: var(--contrast);

    }
  strong{
    font-size: 1.9rem;
    font-weight: 200;
    margin-right: 1rem;
  }
    .work-image {
      width: 40vw;
      height: calc(40vw / 1.618);
      border-radius: 3px;
      margin: 0;
    }
  
    .sr-only {
      color: var(--contrast);
    }
    
    .modifier{
      display: flex;
      justify-content: center;
      align-items: center;
      width: 20%;
    }
}
  </style>
  

  