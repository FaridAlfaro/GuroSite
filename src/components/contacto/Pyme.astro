---

---
<section class="pyme">
  <div class="text">
    <h2>Programa <span>Pyme</span></h2>
    <p>
      Si conocés alguna pyme que creas que necesite de nuestra ayuda, 
      podés colocar sus datos y trataremos de contactarla.
    </p>
  </div>
    
    <form
      data-netlify="true"
      netlify-honeypot="bot-field"
      name="Pyme"
      method="POST"
      action="/success"
      onsubmit="return validarFormulario()"
    >
      <input type="hidden" name="bot-field" />
  
      <!-- TUS DATOS -->
      <h3 class="form-subtitle">Tus datos</h3>
      <div style="margin-bottom: 20px;">
        <label for="tu_nombre">Tu nombre:
          <input type="text" id="tu_nombre" name="tu_nombre" />
        </label>
        <label for="tu_telefono">Tu teléfono:
          <input type="text" id="tu_telefono" name="tu_telefono" />
        </label>
      </div>
  
      <!-- DATOS DE LA PYME -->
      <h3 class="form-subtitle">Datos de la <span>Pyme</span></h3>
      <div style="margin-bottom: 20px;">
        <label for="nombre_empresa">Nombre de la empresa:
          <input type="text" id="nombre_empresa" name="nombre_empresa" />
        </label>
        <label for="red_social">Red social:
          <input type="text" id="red_social" name="red_social" />
        </label>
        <label for="email_empresa">Email:
          <input type="email" id="email_empresa" name="email_empresa" />
        </label>
        <label for="telefono_empresa">Teléfono:
          <input type="text" id="telefono_empresa" name="telefono_empresa" />
        </label>
      </div>
  
      <button type="submit">Enviar</button>
    </form>
  </section>
  
  <script type="module">
    function validarFormulario() {
      const redSocial = document.getElementById("red_social").value.trim();
      const email = document.getElementById("email_empresa").value.trim();
      const telefono = document.getElementById("telefono_empresa").value.trim();
  
      if (!redSocial && !email && !telefono) {
        alert("Por favor completá al menos uno de los siguientes campos: red social, email o teléfono de la empresa.");
        return false;
      }
      return true;
    }
  </script>
    

<style>
    .pyme{
        padding: 0 5%;
        height: 100vh;
        color: var(--background);
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: left;
    }
    .pyme h2, h3{
        font-size: 1.618rem;
        font-weight: 200;
    }
    .pyme h2 span{
        font-style: italic;
        font-weight: 500;
    }
    
    .pyme p{
        font-weight: 300;
        line-height: 1.5;
    }
    .pyme p span{
        font-style: italic;
        font-weight: 500;
    }
    .form-subtitle {
        padding: 1rem;
        background: var(--background);
        color: var(--contrast);
        font-style:normal;
    font-size: 1.618rem;
    font-weight: 200;
    span{
        font-style: italic;
        font-weight: 500;
    }
  }
    #servicios {
        background-color: transparent;
        font-weight: bold;
        border: none;
        margin: 5% 0;
        width: 100%;
        color: var(--background);
    }
    #servicios:focus {
        text-align: left;
        transition: text-align ease 1s;
        color: var(--background);
    }

    form {
        position: relative;
        width: 100%;
        font-weight: normal;
        letter-spacing: 0.1cap;
        font-style: italic;
        font-size: 16px;
    }
    input[type="text"],
    textarea,
    input[type="email"] {
        width: 100%; /* Resta 16px para compensar el padding */
        padding: 8px;
        margin-bottom: 10px;
        border: none;
        border-bottom: 2px solid var(--background);
        box-sizing: border-box;
        background-color: transparent; /* Fondo transparente */
        transition:
            background-color 0.3s,
            border-color 0.3s; /* Efecto de transición */
    }
    input[type="text"]:hover,
    textarea:hover {
        border-color: var(
            --background
        ); /* Cambia el color del borde al hacer hover */
    }
    input[type="email"]:hover,
    textarea:hover {
        border-color: var(
            --background
        ); /* Cambia el color del borde al hacer hover */
    }
    button {
        letter-spacing: 2px;
        width: 100%;
        background-color: transparent;
        color: var(--background);
        padding: 10px 20px;
        border: solid 2px var(--background);
        cursor: pointer;
        background: transparent;
        transition: background-color 0.3s;
    }

    button:hover {
        color: var(--contrast);
        background: var(--background);
    }
    input[type="submit"]:hover {
        background-color: var(--background);
        color: var(--contrast);

    }
    @media (max-width: 700px) {
        input[type="submit"] {
            width: 100%;
        }
    }
    @media (min-width: 1280px) {
      .pyme{
        padding: 0 5%;
        height: 100vh;
        color: var(--background);
        display: flex;
        flex-direction: row;
        justify-content: center;
        align-items: left;
    }
    .pyme h2, h3{
        font-size: calc(1.618rem * 2);
        font-weight: 200;
    }
    .pyme h2 span{
        font-style: italic;
        font-weight: 500;
    }
    
    .pyme p{
        font-weight: 300;
        line-height: 1.5;
        font-size: 2rem;
        width: 70%;
    }
    .pyme p span{
        font-style: italic;
        font-weight: 500;
    }
    .form-subtitle {
        padding: 1rem;
        background: var(--background);
        color: var(--contrast);
        font-style:normal;
    font-size: 1.618rem;
    font-weight: 200;
    span{
        font-style: italic;
        font-weight: 500;
    }
  }
    #servicios {
        background-color: transparent;
        font-weight: bold;
        border: none;
        margin: 5% 0;
        width: 100%;
        color: var(--background);
    }
    #servicios:focus {
        text-align: left;
        transition: text-align ease 1s;
        color: var(--background);
    }

    form {
        position: relative;
        width: 100%;
        font-weight: normal;
        letter-spacing: 0.1cap;
        font-style: italic;
        font-size: 16px;
    }
    input[type="text"],
    textarea,
    input[type="email"] {
        width: 100%; /* Resta 16px para compensar el padding */
        padding: 8px;
        margin-bottom: 10px;
        border: none;
        border-bottom: 2px solid var(--background);
        box-sizing: border-box;
        background-color: transparent; /* Fondo transparente */
        transition:
            background-color 0.3s,
            border-color 0.3s; /* Efecto de transición */
    }
    input[type="text"]:hover,
    textarea:hover {
        border-color: var(
            --background
        ); /* Cambia el color del borde al hacer hover */
    }
    input[type="email"]:hover,
    textarea:hover {
        border-color: var(
            --background
        ); /* Cambia el color del borde al hacer hover */
    }
    button {
        letter-spacing: 2px;
        width: 100%;
        background-color: transparent;
        color: var(--background);
        padding: 10px 20px;
        border: solid 2px var(--background);
        cursor: pointer;
        background: transparent;
        transition: background-color 0.3s;
    }

    button:hover {
        color: var(--contrast);
        background: var(--background);
    }
    input[type="submit"]:hover {
        background-color: var(--background);
        color: var(--contrast);

    }
    }
</style>
