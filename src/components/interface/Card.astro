---
interface Props{
    title: string;
    text:string;
    price: string;
    link: string;
    image: string; // <--- Agrega la propiedad 'image' a la interfaz
}
const {title, text, price, link, image} = Astro.props // <--- Desestructura también la propiedad 'image'
---

<a href={`/service${link}`} class="container">
    <div class="text-container">
        <h3>{title}</h3>
        <span>(About)</span>
        <p>{text}</p>
        <strong class="btn-mod">{price}</strong>
    </div>
    {image && <img src={image} alt={`Imagen para el servicio ${title}`} />}

</a>




<style>
.container img {
        width: 40vw;
        height: 100vh; /* Mantiene la proporción */
        object-fit: cover; /* Asegura que la imagen cubra el área sin distorsionarse */
    }
    .container{
        width: 100%;
        display: flex;
        flex-direction: row-reverse;
        justify-content: space-between;
        color: var();
        animation: side ease-in-out both;
        animation-timeline: view();
        animation-range: 0% 50%;
        margin: 10vh 0;
    }

    @keyframes side {
        from{
            filter: opacity(0);
        }
        to{
            filter: opacity(1);
        }
    }
    
    .text-container{
        height: 100%;
        width: 30%;
        margin: auto;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        justify-content: flex-start;
        color: var(--background);
        h3{
            font-weight: bold;
        }
        p, span{  
            color: var(--gris2);
        }
    }
    @media (max-width: 768px) {
        .container{
            flex-direction: column;
        }
        .container img{
            height: auto;
            width: auto;
        }
        .text-container{
            width: auto;
            height: auto;
            margin: 10% 5%;
        }
    }
</style>